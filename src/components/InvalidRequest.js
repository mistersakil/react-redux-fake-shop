import React from "react";

const InvalidRequest = ({ children }) => {
  return (
    <>
      <h2 className="text-center text-danger">Invalid Request</h2>
    </>
  );
};

export default InvalidRequest;
